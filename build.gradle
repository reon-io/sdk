// Top-level build file where you can add configuration options common to all sub-projects/modules.

ext.projectIds = ['group': 'io.reon', 'version': '0.1-SNAPSHOT']

group = projectIds.group
version = projectIds.version
